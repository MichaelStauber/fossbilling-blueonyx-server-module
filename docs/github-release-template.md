# GitHub Release template

Use this text as the body of a GitHub Release.

## Release summary

`fossbilling-blueonyx-server-module` version `<version>`

## Highlights

- BlueOnyx server module for FOSSBilling
- BlueOnyx hosting plan editor
- Lifecycle integration for BlueOnyx Vsites
- Theme override deployment handled by the module

## Changes

See the matching entry in [`CHANGELOG.md`](../CHANGELOG.md).

## Installation

This release is intended to be installed through the normal FOSSBilling module
workflow or by deploying the repository contents into a matching installation.

## Notes

- Git tag format: `v<version>`
- Release asset: `fossbilling-blueonyx-server-module-<version>.zip`
- The repository is maintained independently from the BlueOnyx core project
